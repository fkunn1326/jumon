chrome.management.getSelf(self => {
    // if (self.installType === 'development') {
    //     chrome.tabs.query({
    //         url: chrome.runtime.getURL('hot-reload.html')
    //     }, tabs => {
    //         tabs.length || chrome.windows.create({
    //             url: '../pages/hot-reload.html',
    //             type: 'popup',
    //             width: 200,
    //             height: 200,
    //             focused: false
    //         });
    //     });
    // }
});